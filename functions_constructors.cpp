//there are 4 types of functions
//1. function : standalone

//2. method : always tied with an object, accessed to an object
//stringname/length() -- length is a method attached to the string object
//can also be attached to a class (static method)

//3. static method : attached to classes

//4. constructor :special function that is called when an object is created
// constructors ahve no return type, it creates an object
//Everytime a user joins, an object is created on the new user (constructor)
//these ensure any new object is ahdering to the rules of the class

//also destructor : special function that is called when an object is deleted
//cleans up the memory of the object